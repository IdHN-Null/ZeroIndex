---
title: "초보자가 자주 하는 C언어 포인터 실수 3가지"
description: "포인터 사용 시 주의해야 할 치명적인 실수들을 정리했습니다."
date: "2026-01-21"
tags: ["c", "pointer", "study", "tip"]
author: "Assistant"
type: "info"
category: "study"
lang: "ko"
slug: "c-pointer-mistakes"
cover: "https://images.unsplash.com/photo-1555066931-4365d14bab8c?q=80&w=2070&auto=format&fit=crop"
---

C언어를 배우면서 가장 어렵고도 중요한 부분이 바로 **포인터(Pointer)**입니다. 강력한 기능을 제공하지만, 그만큼 실수의 여지도 많죠. 이번 글에서는 초보자가 자주 범하는 포인터 실수 3가지를 알아보고, 이를 피하는 방법을 소개합니다.

## 1. 초기화되지 않은 포인터 사용

가장 흔한 실수 중 하나는 포인터를 선언만 하고 초기화하지 않은 채 사용하는 것입니다.

```c
int *p;
*p = 10; // 위험! p가 가리키는 곳이 어디인지 모릅니다.
```

초기화되지 않은 포인터는 메모리상의 임의의 주소를 가리키고 있을 수 있습니다(쓰레기 값). 이 상태에서 역참조(`*p`)를 시도하면 프로그램이 강제 종료되거나 예기치 않은 동작을 할 수 있습니다.

**해결책:** 항상 포인터를 선언할 때는 `NULL` 또는 유효한 주소로 초기화하세요.

```c
int *p = NULL;
int a = 10;
p = &a;
```

## 2. 해제된 메모리 접근 (Dangling Pointer)

`free()` 함수로 메모리를 해제한 후에도, 해당 포인터 변수에는 여전히 해제된 메모리의 주소가 남아있습니다.

```c
char *str = (char *)malloc(10);
free(str);
// ...
strcpy(str, "Hello"); // 위험! 이미 해제된 메모리에 접근 시도
```

이를 **댕글링 포인터(Dangling Pointer)**라고 합니다. 해제된 메모리에 접근하면 데이터 손상이나 보안 취약점이 발생할 수 있습니다.

**해결책:** 메모리를 해제한 직후에는 포인터에 `NULL`을 대입하는 습관을 들이세요.

```c
free(str);
str = NULL;
```

## 3. 포인터 연산의 오해

포인터 연산(`p + 1`)은 단순히 주소값에 1을 더하는 것이 아닙니다. 포인터가 가리키는 **데이터 타입의 크기**만큼 주소가 증가합니다.

```c
int *p = (int *)0x1000;
p++; // p는 0x1001이 아니라 0x1004가 됩니다 (int가 4바이트인 경우)
```

이 점을 간과하고 바이트 단위로 계산하려 하면 잘못된 메모리 위치를 참조하게 됩니다.

---

포인터는 C언어의 양날의 검과 같습니다. 위 3가지 실수만 주의해도 훨씬 안정적인 코드를 작성할 수 있습니다. 더 자세한 내용은 [Study Docs](/docs) 탭에서 확인해보세요!
